import { memo } from "react";
import { Link } from "react-router-dom";

function AddCartMessage({ addItemToCartMessage, setAddItemToCartMessage }) {
  return (
    <div>AddCartMessage</div>
  );
}
export default memo(AddCartMessage);
