import { memo } from "react";

function ProductInfo({
  currentProduct,
  colorValue,
  handleChooseColor,
  sizeValue,
  handleChooseSize,
  handleAddCart,
}) {
  return (
    <>
     <div>ProductInfo</div>
    </>
  );
}
export default memo(ProductInfo);
